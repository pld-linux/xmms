--- xmms-1.2.2/xmms/pluginenum.c.lazy	Thu Jul 20 00:09:17 2000
+++ xmms-1.2.2/xmms/pluginenum.c	Thu Jul 20 00:09:40 2000
@@ -268,7 +268,7 @@
 		   HP-UX does not support dlopen on 32-bit
 		   PA-RISC executables */
 #else
-	if ((h = dlopen(filename, RTLD_NOW)) != NULL)
+	if ((h = dlopen(filename, RTLD_LAZY)) != NULL)
 #endif /* HPUX */
 	{
  
