diff -Nuard xmms-1.2.8.orig/acinclude.m4 xmms-1.2.8/acinclude.m4
--- xmms-1.2.8.orig/acinclude.m4	2003-06-17 15:02:59.000000000 +0200
+++ xmms-1.2.8/acinclude.m4	2003-09-08 03:26:43.000000000 +0200
@@ -214,6 +214,8 @@
 	    AC_PATH_PROG(GNOME_CONFIG,gnome-config,no)
 	    if test "$GNOME_CONFIG" = "no"; then
 	      no_gnome_config="yes"
+	      AM_CONDITIONAL(HAVE_ORBIT,false)
+	      AM_CONDITIONAL(HAVE_GNORBA,false)
 	    else
 	      AC_MSG_CHECKING(if $GNOME_CONFIG works)
 	      if $GNOME_CONFIG --libs-only-l gnome >/dev/null 2>&1; then
@@ -229,6 +231,8 @@
 	      else
 	        AC_MSG_RESULT(no)
 	        no_gnome_config="yes"
+		AM_CONDITIONAL(HAVE_ORBIT,false)
+		AM_CONDITIONAL(HAVE_GNORBA,false)
               fi
             fi
 
@@ -256,6 +260,9 @@
 	          AC_MSG_ERROR(Could not find the gnomeConf.sh file that is generated by gnome-libs install)
  	        fi
 	      fi
+	     else
+	     AM_CONDITIONAL(HAVE_ORBIT,false)
+	     AM_CONDITIONAL(HAVE_GNORBA,false)
             fi
 
 	  if test -n "$3"; then
