diff -Nru xmms-1.2.3/Output/OSS/audio.c xmms-1.2.3.new/Output/OSS/audio.c
--- xmms-1.2.3/Output/OSS/audio.c	Wed Sep 20 05:43:07 2000
+++ xmms-1.2.3.new/Output/OSS/audio.c	Wed Sep 20 05:24:28 2000
@@ -592,7 +592,7 @@
 		else
 			device_name = g_strdup(DEV_DSP);
 	}
-	fd = open(device_name, O_WRONLY);
+	fd = open(device_name, O_WRONLY|O_NONBLOCK);
 	if (fd == -1)
 	{
 		g_warning("oss_open(): Failed to open audio device (%s): %s",
@@ -600,6 +600,7 @@
 		g_free(device_name);
 		return 0;
 	}
+	fcntl(fd,F_SETFL,fcntl(fd,F_GETFL) &~ O_NONBLOCK);
 
 	oss_setup_format(fmt,rate,nch);
 	
diff -Nru xmms-1.2.3/xmms/main.c xmms-1.2.3.new/xmms/main.c
--- xmms-1.2.3/xmms/main.c	Wed Sep 20 05:43:07 2000
+++ xmms-1.2.3.new/xmms/main.c	Wed Sep 20 05:40:48 2000
@@ -464,7 +464,7 @@
 	if (cfg.outputplugin == NULL)
 	{
 #if defined(HAVE_SYS_SOUNDCARD_H) || defined(HAVE_MACHINE_SOUNDCARD_H)
-		cfg.outputplugin = g_strdup_printf("%s/%s/libOSS.so", PLUGIN_DIR, plugin_dir_list[0]);
+		cfg.outputplugin = g_strdup_printf("%s/%s/libesdout.so", PLUGIN_DIR, plugin_dir_list[0]);
 #else
 		/*
 		 * FIXME: This implisitly means the output plugin that is first
